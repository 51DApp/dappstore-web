server:
  port: 8080
  compression:
    enabled: true

management:
  server:
    port: 9090
  endpoints:
    web:
      base-path: /
      exposure.include: "*"
  endpoint:
    health.show-details: always